import React from "react";

export default function App() {
  return (
    <div>
      <span>Hello, Webpack React!</span>
      <button type="button" onClick={() => console.log("clicked")}>
        Click Me
      </button>
    </div>
  );
}
